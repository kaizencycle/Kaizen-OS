# GI consensus attestations (archived)

Historical ATLAS/AUREA/consensus JSON artifacts from C-180–C-193 validation runs.

New attestations should not be written here — use the ledger EPICON path or CI workflow outputs.

Moved from `.gi/` in C-360 PR-C batch 3 (hygiene).
