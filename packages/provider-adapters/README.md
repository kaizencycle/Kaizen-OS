# @mobius/provider-adapters

OpenAI-compatible **chat completions** adapter and **fallback router** (C-285).

Supports `openai`, `openrouter`, and `local` (custom base URL via gateway env).

```bash
npm run build --workspace=@mobius/provider-adapters
```
